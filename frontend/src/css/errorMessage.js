import { StyleSheet } from 'react-native';

const errorStyles = StyleSheet.create({
    errorText: {
    fontSize: 14,
    color: '#ef4444',
    marginTop: -16,
    marginBottom: 16,
    marginLeft: 4,
  }
})

export default errorStyles;